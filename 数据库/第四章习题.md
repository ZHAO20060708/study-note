# 第四章习题

## 4.1

请考虑以下 SQL 查询，该查询旨在查找 2017 年春季讲授的所有课程的标题以及教师的姓名的列表。

```SQL
select name, title
from instructor natural join teaches natural join section natural join course
where semester = 'Spring' and year = 2017
```

请问这个查询有什么问题？

这个查询的主要问题在于 `natural join` 会基于**所有同名属性**进行连接，从而可能引入意外的连接条件，导致结果缺失。

具体来说，在典型的大学数据库模式中，这几个表的常见结构如下：

- `instructor`：ID, name, **dept_name**, salary
- `teaches`：ID, course_id, sec_id, semester, year
- `section`：course_id, sec_id, semester, year, building, room_number, …
- `course`：course_id, title, **dept_name**, credits

注意 `instructor` 和 `course` 表都包含 `dept_name` 列。当执行 `instructor natural join teaches natural join section natural join course` 时：

1. `instructor` 与 `teaches` 自然连接 —— 基于共同列 `ID`，结果包含 `instructor.dept_name`。
2. 再与 `section` 自然连接 —— 基于 `course_id, sec_id, semester, year`，此时 `dept_name` 仍然保留。
3. 最后与 `course` 自然连接 —— 由于此时中间结果既有 `course_id` 又有 `dept_name`（来自 instructor），而 `course` 表中也有 `course_id` 和 `dept_name`，自然连接会要求 **`instructor.dept_name = course.dept_name`** 作为额外的连接条件。

这导致只有那些**教师所属院系与课程开设院系相同**的授课记录才会被保留。如果存在跨系授课（比如计算机系教师教授数学系课程），这些记录会被错误地排除，查询结果将不完整。

**如何修复？** 用显式的 `JOIN ... USING` 或 `JOIN ... ON` 明确指定连接列，例如：

```sql
SELECT name, title
FROM instructor
JOIN teaches USING (ID)
JOIN section USING (course_id, sec_id, semester, year)
JOIN course USING (course_id)
WHERE semester = 'Spring' AND year = 2017;
```

## 4.2

用 SQL 写出下面的查询：

a. 请显示所有教师的列表，展示每位教师的 ID 以及所讲授课程段的编号。对于没有讲授任何课程段的教师，确保将课程段编号显示为 0。你的查询应该使用外连接，且不能使用子查询。  

```SQL
SELECT i.ID, COALESCE(t.sec_id, '0') AS sec_id
FROM instructor i
LEFT OUTER JOIN teaches t ON i.ID = t.ID;
```

b. 请使用标量子查询且不使用外连接来写出 a 中的查询。  

```SQL
SELECT i.ID, 
       (SELECT COALESCE(t.sec_id, '0') 
        FROM teaches t 
        WHERE t.ID = i.ID 
        LIMIT 1) AS sec_id
FROM instructor i;
```

c. 请显示 2018 年春季开设的所有课程段的列表，包括讲授课程段的每位教师的 ID 和姓名。如果一个课程段有不止一位教师讲授，那么有多少位教师，此课程段在结果中就出现多少次。如果一个课程段没有任何教师，它也要出现在结果中，相应的教师姓名被置为 “—”。  

```SQL
SELECT s.course_id, s.sec_id, t.ID, COALESCE(i.name, '—') AS instructor_name
FROM section s
LEFT OUTER JOIN teaches t 
    ON s.course_id = t.course_id 
    AND s.sec_id = t.sec_id 
    AND s.semester = t.semester 
    AND s.year = t.year
LEFT OUTER JOIN instructor i 
    ON t.ID = i.ID
WHERE s.semester = 'Spring' AND s.year = 2018;
```

d. 请显示所有系的列表，包括每个系中教师的总数，不能使用子查询。请确保显示没有教师的系，并用教师计数为零的方式来列出这些系。

```SQL
SELECT d.dept_name, COUNT(i.ID) AS total_instructors
FROM department d
LEFT OUTER JOIN instructor i ON d.dept_name = i.dept_name
GROUP BY d.dept_name;
```

## 4.3

不使用 SQL 外连接（outer join）运算也可以在 SQL 中计算外连接表达式。为了阐明这个事实，请展示如何不使用外连接（outer join）表达式来重写下面的每个 SQL 查询

a. `select * from student natural left outer join takes`

```SQL
SELECT * 
FROM student NATURAL JOIN takes
UNION
SELECT ID, name, dept_name, tot_cred, NULL, NULL, NULL, NULL, NULL
FROM student
WHERE ID NOT IN (SELECT ID FROM takes);
```

b. `select * from student natural full outer join takes`

```SQL
SELECT * 
FROM student NATURAL JOIN takes
UNION
SELECT ID, name, dept_name, tot_cred, NULL, NULL, NULL, NULL, NULL
FROM student
WHERE ID NOT IN (SELECT ID FROM takes)
UNION
SELECT ID, NULL, NULL, NULL, course_id, sec_id, semester, year, grade
FROM takes
WHERE ID NOT IN (SELECT ID FROM student);
```

## 4.4

假设我们有三个关系：r(A, B)、s(B, C) 和 t(B, D)，其中所有属性都声明为非空（not null）。
a. 请给出关系 r、s 和 t 的实例，使得在 (r natural left outer join s) natural left outer join t 的结果中，属性 C 具有一个空值，但属性 D 具有一个非空值。

要让 (r natural left outer join s) natural left outer join t 的结果中 C 为空且 D 不为空，我们需要让 r 中的数据在 s 中找不到匹配项（从而产生空的 C），但能在 t 中找到匹配项（从而产生非空的 D）。

具体实例构造如下：

- 关系 r(A, B) 包含一条记录： A=1, B=2
- 关系 s(B, C) 为空（不包含任何记录）： （无记录）
- 关系 t(B, D) 包含一条记录： B=2, D=3

执行过程验证：

1. 首先计算括号内的 r natural left outer join s：连接属性是 B。因为 s 是空的，r 中的 (1, 2) 在 s 中找不到匹配，所以保留 r 的记录，并将 C 补为空值。 中间结果为：(A=1, B=2, C=null)
1. 然后计算 中间结果 natural left outer join t：连接属性仍然是 B。中间结果中的 B=2，在关系 t 中可以找到匹配的记录 (B=2, D=3)。 最终结果为：(A=1, B=2, C=null, D=3)

在这个结果中，C 是空值，且 D 是非空值，完美满足题目要求。

b. 是否存在 r、s 和 t 的实例，使得在 r natural left outer join (s natural left outer join t) 的结果中，C 有一个空值，但 D 有一个非空值？请解释为什么存在或为什么不存在。

答案：不存在。

解释为什么不存在： 在表达式 r natural left outer join (s natural left outer join t)中，由于有括号，计算顺序发生了改变。

1. 先计算括号内部的连接：Temp = s natural left outer join t。 因为题目已知条件中，所有关系的属性都声明为非空（not null）。所以基础表 s 里的 B 和 C 都是非空的。 当 s 左外连接 t 时，结果集 Temp 中的每一条记录都是以 s 表的记录为基础的。因此，Temp 结果集中的属性 C 绝对不可能出现空值。

1. 再计算外面的连接：r natural left outer join Temp。 此时是以 r 为左表，Temp 为右表进行左外连接，连接条件是属性 B。 对于 r 中的每一条记录，只可能出现两种情况：

- 情况一：在 Temp 中找到了匹配的记录。 那么它就会把 Temp 中的 C 和 D 拿过来。因为前面已经证明了 Temp 中的 C 全部是非空的，所以此时结果中的 C 一定是非空值。
- 情况二：在 Temp 中没有找到匹配的记录。 根据左外连接的规则，右表（Temp）提供的所有属性都要补为空值。也就是说，C 和 D 会同时被设为空值（null）。

结论： 无论数据怎么变化，最终结果里的 (C, D) 要么是“C 非空”，要么是“C 和 D 都是空”。绝对不可能出现“C 为一个空值，但 D 为一个非空值”的情况。这也从侧面证明了，自然左外连接是不满足结合律的。

## 4.5

测试 SQL 查询：为了测试是否正确地用 SQL 写出了一个用文字表达的查询，通常会让 SQL 查询在多个测试数据库上执行，并人工检测每个测试数据库上的 SQL 查询结果是否与用文字表达的意图相匹配。

a. 在 4.1.1 节中我们见过一个错误的 SQL 查询示例，它希望找出每位教师讲授了哪些课程；该查询计算 instructor、teaches 和 course 的自然连接，其结果是它无意地造成了让 instructor 和 course 的 dept_name 属性取值相等。请给出一个数据集的样例，它能有助于捕捉这种特别的错误。

**构建测试数据集样例：**
我们需要构造一个“跨系教课”的场景：

- **instructor (教师表):** `(ID='101', name='Katz', dept_name='Comp. Sci.')` —— Katz 老师属于计算机系。
- **course (课程表):** `(course_id='PHY-101', title='Physics', dept_name='Physics')` —— 物理课属于物理系。
- **teaches (教学表):** `(ID='101', course_id='PHY-101', ...)` —— Katz 老师教这门物理课。

**说明：**
如果是正确的查询（通过 `JOIN ... ON` 指定只匹配 `course_id`），应该能查出 Katz 老师教了 PHY-101。
但是，由于那个错误的 SQL 使用了自然连接，它会去寻找 `dept_name='Comp. Sci.'` 且 `dept_name='Physics'` 的记录，这显然无法匹配，导致查询结果为**空集**。通过预期有一条结果，但实际查出零条结果，测试就能成功捕获这个逻辑错误。

b. 在创建测试数据库时，对每个外码来说，在被引用关系中创建与引用关系中任何元组都不匹配的元组是很重要的。请用大学数据库上的一个查询样例来解释原因。

在数据库里，很多查询需要统计“包括数量为 0 的情况”或“列出所有父级记录，即使它没有关联子记录”。这通常需要使用**外连接 (Outer Join)**。如果测试数据中，每个父级元组都在子表中有对应的引用，那么**内连接 (Inner Join) 和外连接的结果将完全一样**，从而掩盖了没用外连接的语法错误。

**大学数据库样例：**
假设查询要求：“列出所有系的名字，以及每个系的教师总数（包括没有教师的系）”。（这正是你问的第一张图里的 d 小题）。

- **正确写法：** `department LEFT JOIN instructor ...`
- **错误写法：** `department INNER JOIN instructor ...`
如果我们的测试数据库中，**所有的系都至少有一名教师**，那么这两个查询返回的结果行数和数据一模一样。测试就会显示“通过”。
但是，如果我们在 `department` 表中插入一个没有任何教师的系（例如“考古系”），使用错误的内连接写法就会直接漏掉“考古系”，而正确的外连接会显示“考古系：0人”。这样这个 Bug 就暴露出来了。

c. 在创建测试数据库时，倘若外码属性可空，为外码属性创建具有空值的元组是重要的（SQL 允许外码属性取空值，只要它们不是主码的一部分，并且没有被声明为非空（not null））。请用大学数据库上的一个查询样例来解释原因。
提示：请使用来自实践习题 4.2 的查询。

当外码可以为空时，表示一种“没有关联”的业务状态。如果测试数据库中所有的外码都被填满了实际的值，程序员可能会忘记处理 `NULL` 值带来的特殊逻辑（例如忘记使用 `LEFT JOIN`，或者忘记使用 `IS NULL` 判断）。

**大学数据库样例：**
假设在 `student` 表中，有一个外码属性 `advisor_id` 引用 `instructor` 表，允许为空（代表该学生还没有分配导师）。
假设查询要求：“找出所有还没有分配导师的学生的姓名”。

- **正确写法：** `SELECT name FROM student WHERE advisor_id IS NULL;`
如果我们在构建测试数据时，极其“完美”地给每一个学生都分配了 `advisor_id`，那么无论程序员写了什么离谱的查询逻辑，甚至忘了写这个功能，测试数据库中都没有对应的场景来触发这段代码。
只有特意在测试数据中插入一条 `advisor_id = NULL` 的新生记录，我们才能检验系统是否能正确地把这名新生筛选出来。

## 4.6

基于实践习题 3.2 中的查询，请展示如何定义视图 *student_grades* (ID, GPA)，它给出了每名学生的平均绩点；请回忆一下，我们用关系 *grade_points (grade, points)* 来把用字母表示的成绩等级转换为用数字表示的得分。请确保你的视图定义能够正确处理在 *takes* 关系的 *grade* 属性上取空 (null) 值的情况。

```sql
CREATE VIEW student_grades (ID, GPA) AS
SELECT student.ID, 
       SUM(course.credits * grade_points.points) / SUM(course.credits)
FROM student
LEFT JOIN (
    takes 
    JOIN course ON takes.course_id = course.course_id
    JOIN grade_points ON takes.grade = grade_points.grade
) ON student.ID = takes.ID
GROUP BY student.ID;
```

## 4.7

请考虑 4-3 中的员工数据库。请给出这个数据库的 SQL DDL 定义。请指出应该保持的引用完整性约束，并把它们包括在 DDL 定义中。

employee (**ID**, person_name, street, city)
works (**ID**, company_name, salary)
company (**company_name**, city)
manages (**ID**, manager_id)

4-3 员工数据库

关系模式：

- employee（ID, person_name, street, city）
- works（ID, company_name, salary）
- company（company_name, city）
- manages（ID, manager_id）

应保持的约束：

- 主键约束（Primary Keys）
  - employee: ID
  - company: company_name
  - works: ID（假设每位员工只在一家公司工作；若允许多家公司，则应为 (ID, company_name)）
  - manages: ID（假设每位员工只有一个直属经理）

- 外键约束（Foreign Keys / 引用完整性约束）
  - works.ID 必须引用 employee.ID
  - works.company_name 必须引用 company.company_name
  - manages.ID 必须引用 employee.ID
  - manages.manager_id 必须引用 employee.ID（这是一个自引用约束，经理本身也是员工）

对应的 SQL DDL 定义如下：

```SQL
CREATE TABLE employee (
    ID VARCHAR(10),
    person_name VARCHAR(50) NOT NULL,
    street VARCHAR(50),
    city VARCHAR(50),
    PRIMARY KEY (ID)
);

CREATE TABLE company (
    company_name VARCHAR(50),
    city VARCHAR(50),
    PRIMARY KEY (company_name)
);

CREATE TABLE works (
    ID VARCHAR(10),
    company_name VARCHAR(50),
    salary NUMERIC(10,2),
    PRIMARY KEY (ID),
    FOREIGN KEY (ID) REFERENCES employee(ID),
    FOREIGN KEY (company_name) REFERENCES company(company_name)
);

CREATE TABLE manages (
    ID VARCHAR(10),
    manager_id VARCHAR(10),
    PRIMARY KEY (ID),
    FOREIGN KEY (ID) REFERENCES employee(ID),
    FOREIGN KEY (manager_id) REFERENCES employee(ID)
);
```

## 4.8

正如在 4.4.8 节中所讨论的，我们希望能够满足约束“每位教师不能在同一个学期的同一个时间段在两个不同的教室授课”。

a. 请写出一个 SQL 查询，它返回违反此约束的所有 (instructor, section) 组合。

```SQL
SELECT t1.ID, t1.course_id, t1.sec_id, t1.semester, t1.year
FROM (teaches NATURAL JOIN section) AS t1,
     (teaches NATURAL JOIN section) AS t2
WHERE t1.ID = t2.ID 
  AND t1.semester = t2.semester 
  AND t1.year = t2.year 
  AND t1.time_slot_id = t2.time_slot_id
  AND (t1.building <> t2.building OR t1.room_number <> t2.room_number);
```

b. 请写出一个 SQL 断言来强制实现此约束（正如在 4.4.8 节中所讨论的那样，当今的数据库系统并不支持这样的断言，尽管它们是 SQL 标准的一部分）。

```SQL
CREATE ASSERTION no_double_teaching CHECK (
    NOT EXISTS (
        SELECT 1
        FROM (teaches NATURAL JOIN section) AS t1,
             (teaches NATURAL JOIN section) AS t2
        WHERE t1.ID = t2.ID 
          AND t1.semester = t2.semester 
          AND t1.year = t2.year 
          AND t1.time_slot_id = t2.time_slot_id
          AND (t1.building <> t2.building OR t1.room_number <> t2.room_number)
    )
);
```

## 4.9

SQL 允许外码依赖引用同一个关系，如下所示：

create table manager
    (employee_ID    char(20),
     manager_ID     char(20),
     primary key employee_ID,
     foreign key (manager_ID) references manager(employee_ID)
     on delete cascade )

在此，employee_ID 是 manager 表的码，这意味着每位员工最多只有一位经理。外码子句要求每位经理也是一位员工。请准确解释当 manager 关系中一个元组被删除时会发生什么情况。

**当 `manager` 关系中一个元组被删除时，会发生级联删除（Cascading Delete），并且由于是自引用（外码引用自身），这种删除会产生递归的连锁反应。**

具体过程如下：

1. 假设你删除了某个员工的记录（例如经理 A）。
1. 数据库会检查 `manager` 表中所有 `manager_ID` 等于被删除员工的 `employee_ID` 的记录（即查找所有直接向经理 A 汇报的员工）。
1. 因为定义了 `on delete cascade`（级联删除），这些直接下属的记录也会被自动删除。
1. **最关键的是：** 这个过程是递归的。当直接下属的记录被删除时，系统会继续寻找这些下属的下属，并将他们也删除。
1. **最终结果：** 删除任何一个员工的元组，将会自动删除该员工的所有直接下属、间接下属……直到整个由该员工领导的“管理树”分支上的所有员工元组都被彻底删除。

## 4.10

给定关系 a(name, address, title) 和 b(name, address, salary)，请展示如何使用带 on 条件的全外连接（full outer-join）运算来表达 a 自然全外连接（natural full outer join）b，而不使用自然连接（natural join）语法。这可以使用合并（coalesce）运算来完成。请确保结果关系不包含 name 和 address 属性的两个拷贝，并且即使在 a 和 b 中的某些元组对于 name 或 address 属性取空值的情况下，所给出的解决方案也是正确的。

```SQL
SELECT 
    COALESCE(a.name, b.name) AS name,
    COALESCE(a.address, b.address) AS address,
    a.title,
    b.salary
FROM 
    a 
FULL OUTER JOIN 
    b ON a.name = b.name AND a.address = b.address;
```

1. **连接条件：** `FULL OUTER JOIN ... ON a.name = b.name AND a.address = b.address` 执行全外连接，尝试将 `a` 和 `b` 中 `name` 和 `address` 完全相等的元组匹配起来。对于没有匹配上的元组（`a` 中独有的或 `b` 中独有的），也会保留在结果中，另一侧用 NULL 填充。
2. **合并同名列（去重）：** `NATURAL JOIN` 的一个核心特性是同名的连接列在结果集中只出现一次。普通的外连接会产生 `a.name`, `b.name`, `a.address`, `b.address` 四列。使用 `COALESCE(a.name, b.name) AS name` 可以完美解决这个问题。`COALESCE` 函数返回参数列表中第一个非空的值：
   - 如果记录匹配，`a.name` 和 `b.name` 相等，返回该值。
   - 如果是 `a` 中独有的记录（左外），`b.name` 为 NULL，返回 `a.name`。
   - 如果是 `b` 中独有的记录（右外），`a.name` 为 NULL，返回 `b.name`。
3. **处理空值（NULL）：** 即使 `a` 或 `b` 中的 `name` 或 `address` 本身为 NULL，该语句依然正确。
   - 如果 `a` 中某行 `name` 是 NULL 且没有匹配到 `b`，结果中那行的 `a.name` 为 NULL，`b.name` 也因未匹配而为 NULL。`COALESCE(NULL, NULL)` 依然正确返回 NULL，忠实地保留了原始数据结构，不会因为空值而报错或产生多余的列拷贝。

## 4.11

操作系统通常只为数据文件提供两种类型的授权控制：读访问和写访问。为什么数据库系统提供这么多种授权？

数据库的操作语义远复杂于文件的“读/写”。  

- **文件**是无结构的字节流，只需控制**读**和**写**就够了。  
- **数据库**管理的是结构化、关联的数据，必须对**查询、插入、删除、更新、引用、执行、修改结构**等不同操作分别授权，才能实现最小权限和业务安全需求。

## 4.12

假设一个用户想授予另一个用户一个关系上的选择（select）访问权限。为什么该用户应该在授权（grant）语句中包含（或不包含）granted by current role 子句？

包含或不包含 `GRANTED BY current_role` 的关键区别在于**权限的归属和级联撤销行为**：

- **包含该子句**：授权以当前角色（`current_role`）的身份发出。这样，一旦该角色被撤销或其权限被回收，这次授予的权限也会被自动级联撤销，便于统一、动态地管理一组用户的权限。
- **不包含该子句**：授权以用户自身的身份发出，权限独立于角色。即使之后角色变动，已授予的权限依然保留，更适合需要长期稳定、不随角色变化而消失的场景。

以下是图片上的文字识别内容，以及针对这些问题的解答：

### 文字识别

## 4.13

请考虑一个视图 v，其定义仅引用关系 r。

- 如果一个用户被授予 v 上的选择（select）权限，该用户是否也需要在 r 上具有选择权限？试问为什么需要或者为什么不需要？

- **回答：** **不需要。**
- **原因：** 视图的主要作用之一就是**安全性和数据隐藏**。数据库管理员可以通过创建视图，只暴露基础表（关系 r）中的部分行或列给特定用户。如果要求用户必须对底层关系 r 拥有 select 权限才能查询视图 v，那么该用户就可以直接去查询 r，从而绕过视图的安全限制，这使得视图的安全控制机制变得毫无意义。因此，用户只需要对视图 v 有 select 权限即可，DBMS 在执行时会依据视图创建者（或所有者）的权限去访问底层表 r。

- 如果一个用户被授予 v 上的更新（update）权限，该用户是否还需要在 r 上具有更新权限？试问为什么需要或者为什么不需要？

- **回答：** **不需要。**
- **原因：** 逻辑与 select 权限相同。授予用户在视图 v 上的 update 权限，是为了允许他们仅通过视图定义的条件和范围来修改数据，而不允许他们随意修改基础表 r 中的所有数据。如果要求用户拥有 r 的 update 权限，他们就可以绕过视图的限制，直接对基础表进行修改。所以，为了实现精确的存取控制，用户只需在视图 v 上拥有 update 权限，底层数据的实际修改由 DBMS 代理完成。

- 请在视图 v 上给出一个插入（insert）操作的示例，以添加在 select * from v 的结果中不可见的元组 t。请解释你的答案。

- **前提假设：** 假设底层关系为 `r(ID, Name, Salary)`，即员工表。
- **定义视图 v：** 假设视图 v 定义为仅包含工资大于 5000 的员工。

    ```sql
    CREATE VIEW v AS
    SELECT * FROM r WHERE Salary > 5000;
    ```

- **插入操作示例：**

    ```sql
    INSERT INTO v (ID, Name, Salary) VALUES (101, '张三', 3000);
    ```

- **解释：**
    在这个例子中，我们要插入的元组 t 是 `(101, '张三', 3000)`。
    当我们在视图 v 上执行这个插入操作时（假设该视图是可更新的，且没有使用 `WITH CHECK OPTION` 子句），DBMS 会将这条记录实际插入到底层关系表 r 中。
    但是，当我们随后执行 `select * from v` 时，视图的定义条件 `WHERE Salary > 5000` 会生效并过滤数据。由于新插入的员工“张三”的工资是 3000，不满足大于 5000 的条件，因此这条新插入的元组 t **在视图的查询结果中是不可见的**。
    *(注：在实际的 SQL 标准中，如果想防止向视图中插入这种不满足视图条件的“消失的元组”，可以在创建视图时加上 `WITH CHECK OPTION` 约束。但在未加此约束的情况下，上述现象就会发生。)*
